# Al Walima

Site vitrine Next.js — restaurant Al Walima, Agadir.

## Démarrage

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev`  — développement
- `npm run build` / `npm run start` — production
- `npm run lint`

Adresse, carte et horaires : `src/lib/venue.ts`.